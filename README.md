 The readme file will be ready when the project is finished. If you want to contribute to the development, you can contact me, or you can simply clone the repository and run `npm install` and `npm run dev` on the backend and frontend paths. Don't forget to migrate Prisma and run seed. `npx prisma generate` & `npx prisma migrate dev --name init`  or `npx prisma migrate`  & `node prisma/seed.js` or  ` npm run prisma:seed.js`. 

 See u soon :)
